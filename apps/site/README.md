# parlour.vercel.app

The marketing page. One HTML file, one stylesheet, no build step. Vercel
serves this directory as it is, so a change here is live on the next push to
`main`, and every pull request gets a preview.

Edit `index.html` for words and `styles.css` for looks. `vercel.json` only
adds security headers and clean URLs.
