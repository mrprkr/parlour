{
  "name": "site",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/site",
  "targets": {
    "lint": { "command": "pnpm exec biome check apps/site", "options": { "cwd": "../.." } }
  }
}
