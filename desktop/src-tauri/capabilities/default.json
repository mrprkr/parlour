{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The window talks to the Rust side and nothing else. Every capability the app needs, it needs on behalf of the agent, so it happens in Rust where it can be reasoned about.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
