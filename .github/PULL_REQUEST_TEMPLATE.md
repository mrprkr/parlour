## What this does

<!-- Thanks for this. One change, in a sentence or two. Why matters more than what: the diff already says what. -->

## How you tested it

<!-- `pnpm check` is the baseline. Tell us what you tried by hand, on which machine, and anything you could not try (a Linux change from a Mac, for example). -->

## Before merging

- [ ] `pnpm check` passes (typecheck, lint, tests, every project)
- [ ] Anything that can fail silently has a `doctor()` check that names the fix
- [ ] None of your own tokens, keys or hostnames are in the diff
- [ ] Docs are updated if a command, a config key or a path changed
- [ ] British English, no em dashes, in comments and anything the CLI prints
