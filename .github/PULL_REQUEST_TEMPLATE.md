## What this does

<!-- One change, in a sentence or two. Why, more than what: the diff says what. -->

## How it was tested

<!-- `pnpm check` is required. Say what you tried by hand, on which machine, and what you could not try (a Linux change from a Mac, say). -->

## Checklist

- [ ] `pnpm check` passes (typecheck, lint, tests, both projects)
- [ ] Anything that can fail silently has a `doctor()` check that names the fix
- [ ] No token, key or hostname of yours is in the diff
- [ ] Docs updated if a command, a config key or a path changed
- [ ] British English, no em dashes, in comments and anything the CLI prints
