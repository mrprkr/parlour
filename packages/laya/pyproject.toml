[project]
name = "parlour-laya"
version = "0.1.0"
description = "The Python side of Parlour's laya-mlx decision provider, and a local MCP server over the same model"
requires-python = ">=3.11"
dependencies = [
  "laya-mlx==0.2.0",
  "mcp>=2.2,<3",
]

[tool.uv]
package = false
