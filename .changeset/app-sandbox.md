---
"parlour": minor
---

Run inside the Mac App Store app's sandbox: secrets stay in the container, Homebrew and launchd are left alone, and `parlour start` keeps whisper and the local model warm itself.
