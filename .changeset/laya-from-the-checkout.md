---
"parlour": patch
---

The laya-mlx decision provider uses the repository's pinned environment in packages/laya when run from a checkout, so `pip install laya-mlx` is only needed for an npm install.
