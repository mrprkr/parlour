---
"parlour": minor
---

Add an optional `ha_assist` tool (`assist: true`, off by default) that hands a command to Home Assistant's Assist, so custom sentences and sentence-triggered automations fire when Parlour is the one listening.
